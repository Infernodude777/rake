# BYO API Keys are configured inside the app under Settings > API Keys
# and stored in your browser's localStorage.

# No external service configuration required.
# Just run `npm install && npm run dev` to start.
